<%= form_with(model: purchase_item) do |form| %>
  <% if purchase_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(purchase_item.errors.count, "error") %> prohibited this purchase_item from being saved:</h2>

      <ul>
        <% purchase_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :purchase_id, style: "display: block" %>
    <%= form.text_field :purchase_id %>
  </div>

  <div>
    <%= form.label :digital_asset_id, style: "display: block" %>
    <%= form.text_field :digital_asset_id %>
  </div>

  <div>
    <%= form.label :price_at_purchase, style: "display: block" %>
    <%= form.text_field :price_at_purchase %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
